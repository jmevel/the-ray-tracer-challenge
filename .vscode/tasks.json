{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Test",
            "type": "shell",
            "command": "cargo test --test example"
        }
        {
            "label": "Test (disabled concurency)",
            "type": "shell",
            "command": "cargo test --test example -- --concurrency=1"
        }
    ]
}